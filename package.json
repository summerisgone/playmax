{
  "name": "playmax",
  "version": "1.0.0",
  "scripts": {
    "browser": "bun browser.ts",
    "list-chats": "bun list-chats.ts",
    "read-history": "bun read-history.ts",
    "start": "bun index.ts",
    "test": "bunx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
